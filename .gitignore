__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.venv/
build/
dist/
/freens
/freens-cli

# register artifacts (written to cwd by default)
*.tld.cbor

# key bundles (`freens backup`) — NEVER commit: anyone holding one owns the names
freens-backup-*.tar.gz

# agent working notes (private host details)
AGENTS.md

# windows build artifacts
freens.exe
freens-cli.exe
freens-web.exe
