"""freens — Free Namespace reference implementation (v0.1 draft).

See specifications.md for the protocol. This package implements the
protocol-critical core: naming model, wire format, cryptography, claims,
DHT data structures, and the resolver config. Public submodules:
freens.constants, freens.naming, freens.cbor_canon, freens.crypto,
freens.wire, freens.claims, freens.dht, freens.resolver_config.
"""

__version__ = "0.1.0"
